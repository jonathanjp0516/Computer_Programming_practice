#include<stdio.h>
#include <stdbool.h>

float fact(const int num){
    if(num==1){
        return 1;
    }else{
        return num * fact(num - 1);
    }
}

int main(){
    int num = 0,sum = 0;
    while(1){
        scanf("%d",&num);
        for(int i=0;i<=num;i++){

        }
    }
}